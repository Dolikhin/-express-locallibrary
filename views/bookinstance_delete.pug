extends layout

block content
  h1 #{title}: #{bookinstance.book}
  
  p Вы действительно хотите удалить этот экземпляр?

  form(method='POST' action='')
    div.form-group
      input#authorid.form-control(type='hidden',name='bookinstanceid', required='true', value=bookinstance._id )

    button.btn.btn-primary(type='submit') Удалить
